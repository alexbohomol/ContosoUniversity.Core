apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cuweb-ingress
  namespace: contoso
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cuweb-service
            port:
              number: 80
