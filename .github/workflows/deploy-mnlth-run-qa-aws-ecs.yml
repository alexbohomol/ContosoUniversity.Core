name: "MNLTH / Deploy / Run qa-aws-ecs"

on:
  workflow_dispatch:

jobs:
  run:
    name: Run
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Run the deploy
        run: |
          echo "Run the deploy"
